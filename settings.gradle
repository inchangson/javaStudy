pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'java-study'

include 'topics:oop'
include 'topics:virtual-thread'

include 'topics:map-storage'

include 'topics:connection-pool'
